# Session Artifact — 2026-04-08

> **Purpose:** Context recovery document. Read this to resume work on this project.
> **NOT a wiki page.** Lives in docs/, not wiki/. Do not ingest.

---

## What Was Built

### Phase 1: Research Wiki Core (Session 1)
- **Spec:** `docs/superpowers/specs/2026-04-08-research-wiki-design.md`
- **Plan:** `docs/superpowers/plans/2026-04-08-research-wiki-implementation.md`
- Scaffolded monorepo: raw/, wiki/, tools/, skills/, config/, scripts/
- Created `CLAUDE.md` project brain
- Built 6 Python tools: validate, manifest, lint, export, stats, obsidian
- Built wiki-agent skill (`skills/wiki-agent/skill.md`)
- 42 tests, all passing

### Phase 2: Setup Scripts + Obsidian + NotebookLM (Session 1)
- **Spec:** `docs/superpowers/specs/2026-04-08-setup-obsidian-notebooklm-design.md`
- **Plan:** `docs/superpowers/plans/2026-04-08-setup-obsidian-notebooklm.md`
- Built 8 bash scripts: lib, setup, install-deps, install-obsidian, configure-obsidian, configure-exports, setup-notebooklm, sync-obsidian
- Created `tools/obsidian.py` (wikilink generator for graph view)
- Created NotebookLM stub skill

### Phase 3: Ingestion Pipeline + Content (Session 1)
- Built `tools/ingest.py` — automated URL fetching (YouTube, GitHub, web)
- Ingested 15 raw sources (4 YouTube + 8 GitHub + 2 transcripts + 1 note)
- **Pass 1:** 29 pages, 176 relationships
- **Pass 2:** Cross-reference → 31 pages, 222 relationships
- **Pass 3:** Ecosystem cross-reference → 42 pages, 323 relationships

### Phase 4: Ecosystem Documentation (Session 2)
- Scanned all 4 local sister projects via subagents (openfleet, AICP, control-plane, DSPD)
- Researched OpenClaw, LightRAG, Plane, Obsidian CLI, notebooklm-py online
- Created 11 concept pages from directives + local + online research
- Fixed lint issues (dead relationship, orphan comparisons)
- Created devops domain (new), comparisons/_index.md

### Phase 5: Pipeline Orchestration (Session 2)
- **Spec:** `docs/superpowers/specs/2026-04-08-pipeline-automation-design.md`
- Built `tools/pipeline.py` — pipeline orchestrator with:
  - Post-chain (5 steps in one command)
  - Parallel fetch (ThreadPoolExecutor)
  - Gap analysis pipeline
  - Cross-reference analysis pipeline
  - 9 named chains (ingest, ingest-local, analyze, full, health, publish, mirror, research, deep)
  - Integration steps (notebooklm mirror, research, ask, sync)

### Phase 6: Services + Integrations (Session 2)
- Built `tools/integrations.py` — Obsidian CLI + notebooklm-py wrappers with graceful fallback
- Built `tools/sync.py` — WSL↔Windows sync daemon (rsync/robocopy/shutil, bidirectional, watch mode)
- Built `tools/watcher.py` — wiki change detection → auto post-chain, auto sync
- Built `tools/setup.py` — cross-platform setup (Windows + Linux + macOS)
- Built `tools/mcp_server.py` — 13 MCP tools for Claude Code native integration
- Created `.mcp.json` for auto-discovery
- Set up project venv with uv + Python 3.11
- Installed notebooklm-py 0.3.4 + mcp SDK

## Current Wiki Stats

```
Pages: 42 (26 concepts, 3 comparisons, 12 source-syntheses)
Domains: ai-agents (9), knowledge-systems (9), automation (6),
         tools-and-platforms (14), devops (1), cross-domain (2)
Relationships: 323 (7.7 avg/page)
Tags: 150+ unique
Sources: 23 raw files (6 transcripts, 13 articles, 3 notes, 1 dump)
Validation: 0 errors
Lint: 3 minor (index nav page, 2 small domains)
```

## Entry Points

### Pipeline (primary — use `.venv/bin/python`)
| Command | Purpose |
|---------|---------|
| `pipeline post` | Post-ingestion chain (index → manifest → validate → obsidian → lint) |
| `pipeline fetch URL [URL...]` | Fetch URLs into raw/ |
| `pipeline run URL [URL...]` | Parallel fetch + post-chain |
| `pipeline gaps` | Gap analysis (orphans, thin pages, weak domains, open questions) |
| `pipeline crossref` | Cross-reference analysis (backlinks, bridges, comparisons) |
| `pipeline chain health` | Post → gaps → crossref |
| `pipeline chain publish` | Post → sync to Windows |
| `pipeline chain full URL...` | Fetch → post → gaps → crossref → sync |
| `pipeline chain mirror` | Push wiki sources to NotebookLM |
| `pipeline chain deep` | Gaps → crossref → mirror → post → sync |
| `pipeline chain --list` | List all 9 chains |
| `pipeline status` | Show raw files + wiki page count |
| `pipeline integrations` | Check Obsidian CLI + notebooklm-py status |

### Services
| Command | Purpose |
|---------|---------|
| `watcher --watch` | Daemon: detect changes, auto post-chain |
| `watcher --watch --sync` | Same + auto-sync to Windows |
| `sync` | One-shot rsync wiki/ → Windows |
| `sync --watch` | Daemon: bidirectional sync on changes |
| `sync --reverse` | Pull changes from Windows → WSL |

### MCP Server (13 tools, registered in .mcp.json)
wiki_status, wiki_search, wiki_read_page, wiki_list_pages,
wiki_post, wiki_fetch, wiki_fetch_topic, wiki_scan_project,
wiki_gaps, wiki_crossref, wiki_sync, wiki_mirror_to_notebooklm,
wiki_integrations

### Setup
| Command | Purpose |
|---------|---------|
| `python -m tools.setup` | Full cross-platform setup |
| `python -m tools.setup --check` | Check environment |
| `python -m tools.setup --deps` | Install deps via uv |

### Individual tools
validate, manifest, lint, export, stats, obsidian, ingest

## User Directives — ALL COMPLETED

All directives from `raw/notes/` have been implemented:

1. **Ecosystem Documentation** ✅ — 11 pages covering full toolchain
2. **Pipeline Automation** ✅ — tools/pipeline.py with chains, groups, parallel execution
3. **Chain/Group/Tree Operations** ✅ — 9 named chains, ThreadPoolExecutor for parallel fetch
4. **Integrate Obsidian CLI + notebooklm-py** ✅ — tools/integrations.py with fallback
5. **WSL↔Windows rsync** ✅ — tools/sync.py daemon with bidirectional watch
6. **Wiki watcher** ✅ — tools/watcher.py with auto post-chain
7. **MCP Servers** ✅ — tools/mcp_server.py with 13 tools
8. **Bidirectional sync** ✅ — watcher + sync --watch
9. **Claude becomes replaceable** ✅ — all operations exposed as CLI + MCP, any LLM can operate

## Architecture

```
User / Claude Code / Any MCP Client
         │
    ┌────▼────────────────────────────────────────┐
    │  MCP Server (tools/mcp_server.py)            │
    │  13 tools — stdio transport                  │
    └────┬────────────────────────────────────────┘
         │
    ┌────▼────────────────────────────────────────┐
    │  Pipeline (tools/pipeline.py)                │
    │  9 chains — post, fetch, gaps, crossref,     │
    │  mirror, sync, publish, research, deep       │
    └────┬──────────┬──────────┬──────────────────┘
         │          │          │
    ┌────▼───┐ ┌────▼───┐ ┌───▼──────────────────┐
    │ Tools  │ │Integr. │ │ Services              │
    │validate│ │obsidian│ │ watcher (auto post)   │
    │manifest│ │notebklm│ │ sync (WSL↔Windows)    │
    │lint    │ │        │ │                        │
    │export  │ └────────┘ └────────────────────────┘
    │stats   │
    │obsidian│
    │ingest  │
    └────────┘
```

## Cross-Session Memory

Stored in `~/.claude/projects/-home-jfortin-devops-solutions-research-wiki/memory/`:
- `user_profile.md` — DevOps engineer, 4-project ecosystem
- `feedback_verbatim_logging.md` — Log all directives verbatim
- `feedback_use_superpowers.md` — Always use superpowers proactively
- `feedback_pipeline_not_manual.md` — Solve blockers with tooling
- `feedback_iterative_depth.md` — Multi-pass ingestion
- `feedback_slow_down_follow_order.md` — Follow directive order, don't skip steps
- `project_backlog_ecosystem.md` — COMPLETED
- `project_backlog_services.md` — COMPLETED

## How to Resume

1. Read this file for context
2. Read `CLAUDE.md` for project conventions and all commands
3. Run `.venv/bin/python -m tools.pipeline chain health` for current wiki state
4. Run `.venv/bin/python -m tools.pipeline integrations` to check tool availability
5. All directives are implemented — ask user for next priorities
6. Potential next steps:
   - Ingest new sources (user provides URLs/topics)
   - Deepen existing pages (use `pipeline gaps` to find targets)
   - Export to sister projects (`tools/export.py openfleet`)
   - Set up notebooklm login for mirror/research chains
   - Move primary dev environment to Windows for Obsidian CLI
