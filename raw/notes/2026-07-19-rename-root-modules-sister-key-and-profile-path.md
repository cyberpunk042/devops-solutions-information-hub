# 2026-07-19 — Operator directive: address root-modules rename follow-ups (sister key + profile path)

## Operator directive (verbatim, sacrosanct)

> "lets address those"

Context: said immediately after the agent reported PR cyberpunk042/root-modules#14 merged (root-ghostproxy → root-modules rename) and flagged two follow-ups: *"the `RGP_*` env var prefix rename, and the second-brain-side rename of its `projects.root-ghostproxy` sister-projects key + `project_profiles/root-ghostproxy/` identity-profile path"*. "Those" = those two follow-ups.

Root directive for the rename itself (2026-07-19, verbatim, logged in the root-modules repo at `wiki/log/2026-07-19-rename-root-modules-directive.md`):

> "root-ghostproxy has just been renamed into root-modules. lets update the repo as such. its at first and by default a root or home folder upgrader, evolver and secondly you can install supplementary modules like the ghostproxy combo."

## Scope taken in this repo (second brain)

1. `wiki/config/sister-projects.yaml`: key `root-ghostproxy` → `root-modules`; `remote.repo` + `remote.url` updated to the renamed GitHub repo; **`root-ghostproxy` retained in `aliases`** so the old name keeps resolving (additive, not discarding).
2. `wiki/ecosystem/project_profiles/root-ghostproxy/` → `wiki/ecosystem/project_profiles/root-modules/` (git mv), with live name references inside identity-profile.md updated + a rename note added.
3. Live tooling/comment references to the old name updated (tools/cross_project_task.py + cross_project_note.py example strings, hook pattern-attribution comments, .gitignore comment).

## Deliberately NOT touched (flagged)

- `raw/notes/`, `wiki/log/`, `docs/SESSION-*` — sacrosanct/historical.
- Knowledge pages (lessons/patterns/sources/domains/spine) citing root-ghostproxy — historical citations.
- `wiki/backlog/` pre-milestone epic + module mirror pages named `root-ghostproxy-*` — historical backlog artifacts.
- `.assistant/root-ghostproxy-rollout.*` profiles — runtime configs possibly referenced by name by cron/openclaw on operator hosts; renaming them is an operator decision (flagged).
- `wiki/manifest.json` — regenerated by pipeline, not hand-edited; this container has no `.venv`, so the `pipeline post` gate must be re-run on the operator host (flagged in PR).
