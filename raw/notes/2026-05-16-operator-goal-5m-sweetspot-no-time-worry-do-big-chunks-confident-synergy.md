---
title: "2026-05-16 — Operator /goal: 5m cron sweet spot + agent not directed to worry about time + do big chunks of work + workflow + synergy confidence"
type: note
note_type: directive
domain: log
status: active
confidence: authoritative
created: 2026-05-16
updated: 2026-05-16
sources:
  - id: operator-goal-2026-05-16-evening-2-cadence-doctrine
    type: directive
tags: [operator-directive, root-ghostproxy, ai-assistant-profile, cadence-doctrine, 5m-sweetspot, do-big-chunks, no-time-worry, workflow-respect, synergy-confidence, sacrosanct, "2026-05-16"]
---

# Operator /goal — 5m cron sweet spot + do big chunks of work + no time-worry + synergy confidence

## Verbatim operator words (sacrosanct, 2026-05-16 evening 2)

> "5m is probably the sweetpot for an active worker main CRON and the agent
> is not directed to worry about time. to not minimize and do big chunks
> of work and respect the workflow and etc... this is also important
> otherwise what could take 1h would take 4-5 hours.... its complex but
> little but little. We apply the knowledge of the second-brain to keep
> improving. so lets continue till we fill confident into our triggers,
> prompts and directives and the synergy."

## Parsed constraints (the spec the v5.2 cadence-doctrine augmentation must satisfy)

1. **5m cron cadence is the sweet spot for the worker's main cron (driven-worker-tick).** Was 15m; change to 5m.

2. **The agent is NOT directed to worry about time.** Per-fire prompts MUST NOT contain time-budget caps that would cause the worker to fragment substantive work. The worker picks the right-sized task (Goldilocks scope-sizing) THEN DOES IT FULLY.

3. **Do not minimize; do big chunks of work; respect the workflow.** Substantive depth + workflow discipline (SDD+TDD per task + SFIF recursive per scope + augment-not-rewrite + R20) are the WORK SHAPE, not time-constrained chunks.

4. **Otherwise what could take 1h would take 4-5 hours.** Time-anxiety = work-fragmentation = ~4-5× wasted overhead (context-rebuild + re-orient + lock-acquire-cycle + stage-and-restage per chunk). The doctrinal answer: TRUST the lock + 5m cadence to handle overlap; DON'T try to make the agent worry about time.

5. **Complex but little by little.** Iterative; not "rewrite the profile from scratch each cycle". Per augment-not-rewrite (operator 2026-05-16 prior).

6. **We apply the knowledge of the second-brain to keep improving.** Each profile evolution cites + binds to second-brain artifacts (super-models / models / standards / methodology / SFIF / Goldilocks / SDD+TDD profiles / per-type page-standards). Per worker_cadence_doctrine.do_big_chunks_directive.encoded_in.

7. **Continue till we feel confident into our triggers, prompts, directives, and the synergy.** Goal is a quality state, not a fixed item count. The trigger × prompt × directive × workflow synergy is the confidence-target.

## Concrete changes this /goal triggered (v5.2 cadence-doctrine arc)

24 surgical edits across 3 profile files + 1 new top-level `worker_cadence_doctrine` block + this verbatim directive log + 1 session log at wiki/log/ = the spec evolution per this /goal.

Per session-prior operator directive `raw/notes/2026-05-16-operator-directive-focus-profile-not-openclaw-do-not-decide-do-not-minimize-workflow.md`: NO OpenClaw runtime touches. R20 sacrosanct throughout (zero commits + zero `git rm` on tracked files).

## Status

Logged verbatim. Profile v5.2 cadence-doctrine arc complete. Synergy verification via grep audit confirms only intentional historical references remain (`every:3m hardcoded` in the v1-anti-pattern-REMOVED comment). Pipeline post pending.
