---
title: "2026-05-09 — Operator directive (turn 8): common cross-project user surface — `.cursor/`-style gitignored state folder for /view + /questions + vision + status + tracking + trace + progress + focus + mission; auto-regenerated; per-system not cross-system; overlaps with root-ghostproxy cursor files; aligned via root-propagated global commands"
type: note
note_type: directive
domain: cross-domain
status: raw
confidence: high
created: 2026-05-09
updated: "2026-05-09"
sources:
  - id: operator-directive-2026-05-09-turn-8
    type: directive
    description: "Operator-stated directive 2026-05-09 turn 8 — common cross-project user surface (gitignored state folder) for /view + /questions + tracking; root-ghostproxy alignment; auto-regenerated by assistants; per-system temp; pasted full /view skill definition verbatim as authoritative reference"
tags: [operator-directive, sacrosanct, verbatim, "2026-05-09", common-cross-project-surface, cursor-files-pattern, gitignored-state-folder, view-questions-vision-mission-tracking, auto-regenerated-by-assistants, root-ghostproxy-propagation-alignment, raw-note]
---

# Operator directive — 2026-05-09 turn 8: common cross-project state surface

## Verbatim (operator, sacrosanct)

> "continue and also:
> maybe we can have a common user surface accross project so that for instance if I want to see the state of the progress I can see the snapshot of the /view (you dont know about yet) and /questions (you probably dont know about yet either) but that allow me to see where we are and see if there are pending question and even enter mode like a interactive mode to solve the questions, well if all Agent mode and AI Assistant mode and platform driven agent can know and agrees on this and if the global commands that will be imposed globally from root-ghostproxy and align all this we will be able to have a common surface not only for the Wiki LLM and PM inside it but also the vision / status / tracking / current trace and progress and focus and mission and etc that can also be regenerated / updates sometimes even automatically. This folder can possibly be a gitignored folder so that we are aware its a temp folder that we dont necessarily manage directly and that its more about managing for on one system, not accross multiple computer. This also avoid git conflict. this overlap a bit or connect ratther with the cursor files of the root-ghostproxy."

> [Operator then pasted the full `/view` slash command definition — see `.claude/commands/view.md` reference if propagated, or the verbatim block in the chat transcript. The pasted definition is the authoritative source for what `/view` does.]

## Decomposition

### A — Common cross-project surface
- Surface = state-snapshot folder accessible by `/view`, `/questions`, and other operator commands
- SAME structure across projects (one convention, many projects)
- Anchors: vision · status · tracking · current trace · progress · focus · mission

### B — /view and /questions are operator's existing slash commands
- `/view` definition pasted by operator — authoritative; 7-section command-center render
- `/questions` is interactive mode for resolving pending questions
- BOTH likely root-propagated (per the agent-config-propagation memory) — NOT /opt's authoring scope

### C — Auto-regeneration
- Content "regenerated / updates sometimes even automatically"
- Implication: an assistant Profile (or CRON job) keeps the folder fresh

### D — Gitignored + per-system
- "gitignored folder so that we are aware its a temp folder"
- "more about managing for on one system, not across multiple computer"
- "This also avoid git conflict"
- Per-system runtime cache, not cross-machine state

### E — Root-ghostproxy alignment
- "global commands that will be imposed globally from root-ghostproxy"
- "this overlap a bit or connect rather with the cursor files of the root-ghostproxy"
- The slash commands (and their conventions) come FROM root-ghostproxy and propagate to all projects
- /opt's job: ensure the state folder + content are present for the propagated commands to consume

### F — Multi-consumer alignment
- "all Agent mode and AI Assistant mode and platform driven agent can know and agrees on this"
- The state surface must be readable by ANY of: Claude Code agent · OpenClaw agent · Multica agent · operator directly · etc.
- This means the format must be tool-agnostic (markdown / json / yaml)

### G — Surface scope
- "not only for the Wiki LLM and PM inside it" — broader than just wiki + backlog
- ALSO: vision · status · tracking · current trace · progress · focus · mission
- This is the project's "operating state" — what would you read to know "where are we"

## What /opt should author (boundary check)

| /opt-territory | NOT /opt-territory |
|---|---|
| The `.cursor/` folder structure | The `/view` slash command (root-propagated) |
| The state files inside (`view-snapshot.md`, `questions-snapshot.md`, `vision.md`, etc.) | The `/questions` slash command (root-propagated) |
| The regeneration tool (`tools/state.py` or similar) | Cross-project state propagation logic (root-ghostproxy's job) |
| `.gitignore` entry for `.cursor/` | Global command conventions (root-propagated) |
| `wiki/spine/standards/cursor-state-folder-standards.md` (the standards doc — knowledge layer) | The slash-command implementations themselves |
| `.assistant/_global/cron.yaml` job for periodic regeneration | (boundary respected) |

## Action plan

| # | Action | Status |
|---|---|---|
| 1 | Log this directive verbatim BEFORE acting (this file) | ✅ done |
| 2 | Create `.cursor/` folder skeleton with README + initial snapshot files | pending |
| 3 | Add `.cursor/` to `.gitignore` | pending |
| 4 | Author `tools/state.py` — regenerator for `.cursor/` content from project sources | pending |
| 5 | Add `regenerate-cursor-state` global CRON job to `_global/cron.yaml` | pending |
| 6 | Author `wiki/spine/standards/cursor-state-folder-standards.md` (the convention as knowledge) | pending |
| 7 | Cross-link from `.assistant/README.md` to the state-folder docs | pending |
| 8 | Pipeline post + report | pending |

## No-conflate guard

- **/view and /questions exist** — operator pasted /view; /questions exists too. Do NOT author them; they're root-propagated.
- **Cursor files overlap** — operator named "cursor files of the root-ghostproxy". /opt's folder may use the same name (`.cursor/`) for alignment, or a project-local name to avoid conflict. Picking `.cursor/` per operator's wording; document the alignment expectation.
- **Auto-regeneration is a CRON job** — not a separate Profile (small mechanical work); but the regeneration logic can READ from existing wiki + git + backlog (no new data sources).
- **Per-system temp** — `.gitignore` entry essential; data is regenerable from project sources, so loss of `.cursor/` is recoverable.
- **Multi-consumer** — content is markdown (operator-readable, AI-readable, no special format).

## Forward chain

- BUILDS ON: [[2026-05-09-operator-correction-turn-7-not-done-til-script-spawns-live-assistant-with-multi-surface-cron-and-command-equivalents|turn 7 directive]] (the lifecycle management system)
- EXTENDS: `.assistant/` infrastructure with a sibling `.cursor/` (project-state runtime cache)
- ALIGNS WITH: root-ghostproxy cursor-files convention (operator-stated)
- ENABLES: operator's `/view` and `/questions` slash commands to render against /opt's state quickly
