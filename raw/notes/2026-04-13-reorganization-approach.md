# Operator Directive: Reorganization Approach
Date: 2026-04-13

## Verbatim

"Would it not be more optimal if you were updating the links / refs at the same time?"
"Also super-models can have their own folder and each one can have a proper name instead of a generic one like the folder.... and all models their can be split into appropriate folder group if its still over 10 or because we will probaly have to add more as we go. lets keep thinking like this and make sure we do things right.. not that I had anything wrong with the last command as a start."

## Requirements

1. Move files AND update all [[filename|title]] wikilinks in a single pass
2. Super-model + sub-models get their own folder (not generic name)
3. Models split into appropriate folder groups (anticipate growth)
4. Think ahead — folders should accommodate future additions
5. The clustering approach was right, just needs to be atomic (move + relink together)
