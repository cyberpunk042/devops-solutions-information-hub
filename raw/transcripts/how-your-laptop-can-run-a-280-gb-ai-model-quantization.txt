# How Your Laptop Can Run a 280 GB AI Model  |  Quantization

Source: https://www.youtube.com/watch?v=Quj3M5gqxT8
Ingested: 2026-05-08
Type: youtube-transcript

---

70 billion numbers, each one four bytes wide, 280 GB bigger than any laptop, bigger than four data center GPUs combined. And yet, this exact model runs on your laptop, and it barely loses any quality. The technique is called quantization, and the obvious version of it doesn't work. In 2022, Tim Deetm's published this chart. The horizontal axis is model size. The vertical axis is quality. The green line is the original 16- bit model, climbing steadily as models grow. Now watch the orange line. This is the naive 8-bit version. For small models, it tracks the original perfectly until it doesn't. At 6.7 billion parameters, something breaks. The orange line drops off a cliff. The dashed red line marks the moment when in Demmer's words, the outliers emerge. The same procedure that worked for smaller models has destroyed quality at scale. To understand why, we need to look at what's actually inside an LLM. Here's the actual distribution of weights in a real Mstral 7 billion language model. Each weight is a number, and there are millions of them. Look at where they live. At first glance, this might look like just another histogram. But notice the shape. Almost all of them are tiny clustered close to zero. The peak is about 550,000 weights packed into the central bin. The two orange dashed lines mark the boundary. The largest and smallest values around plus or minus 0.068. The whole shape is a bell curve. This is not an accident. It happens reliably in every modern LLM in every layer. If most weights are tiny, we don't need precision out at the edges. We need it where the weights actually are. Here's what we are about to do. Replace each 32-bit weight with a 4bit code. Four bits gives us only 16 distinct values, like a 16-let alphabet that has to spell every weight in the model. So, here's the question. If you had only 16 grid points to place across this bell curve, where would you put them? Hold that thought. Those 16 grid points will be back. To see the mechanism on numbers, we'll start with int8 8 bit where the math is cleanest. Then drop to four bits when outliers force it. We are now going to quantize from FP32 to int8 then dequantize back through a worked example. FP32 reaches plus orus 3.4 4 * 10 38th and astronomical range. Int 8 is just plus or minus 127. At its heart, quantization is two formulas. To quantize, divide the weight by a scale and round. To dequantize, multiply the integer by the scale. Let's see this on numbers. Take a tiny tensor. 3.2, -2.1, 0.5, and 1.8. 432 bit floats, 16 bytes total. We find the scale by looking at the largest absolute value 3.2. Map it to the largest signed 8 bit integer 127. The scale is 3.2 / 127 about 0.0252. Now quantize 3.2 / the scale equals 127 which rounds to 127. -2.1 becomes83. 0.5 becomes 20. 1.8 becomes 71. Four floats now four integers, one bite each. Four bytes total, a quarter of the original. To recover, multiply each integer by the scale. 127* the scale gives 3.20.83 gives -2.091. 20 gives 0504. 71 gives 1.789. The errors are tiny, around a hundredth of a unit. Take a moment. We compressed four numbers to a quarter and recovered them with errors invisible to the eye. In a real LLM, the scale is amvertised across many weights. So the per weight overhead becomes tiny and we approach four times compression at 8 bits. So the formula works on a clean little tensor. But what happens on a real LLM weight matrix? Same procedure. Well, the same formulas anyway, but now on a tensor of 1,024 real weights. 1,023 of them sit in the range plus orus 2 like a normal bell curve. But one weight, just one, is plus 30. Apply the same formula we just used and watch what happens. The abs max is no longer 2, it's 30. The scale becomes 30 / 127. About 0.236. Watch the bulk. Of the 256 available 8 bit values, only about 27 are actually used. 229 are wasted on a region the bulk weights never visit. The effective resolution in the bulk has collapsed from 8 bits to about five. One outlier set the scale. The rest of the weights paid the price in loss precision. And remember Tim Demer's chart at 6.7 billion parameters. Every layer of every modern LLM develops outliers like this on the same handful of feature dimensions with magnitudes around 100 times the average. Outliers are not a rare bug. They are the central villain that every modern algorithm exists to handle. How do we deal with the villain? The simplest fix is to stop using one scale for the whole tensor. Take that same 1,024 weight tensor and slice it into 16 groups of 64 weights. Each group gets its own absmax and its own scale. The outlier sits in just one of those groups. That group still has its course scale around 0.236. But the other 15 groups have abs around three. Their scale is 3 / 127. about 0.024, 10 times finer than the global scale. The bulk regains its natural precision. The cost is small, one extra scale per 64 weights, about a quarter of a bit per weight in metadata. Every modern 4-bit format does this. Let's zoom into one of those groups and look at what's left inside. Inside each group, we still have only 16 grid points. Remember the question from the intuition scene? Where would you place 16 grid points across a bell curve? The simple answer is 16 evenly spaced points, but that wastes most of them at the edges and crowds only one at the dense center. Here is Qura's answer called NF4. There's something genuinely elegant about it. It places 16 points at the quantiles of a normal distribution. The result is a nonuniform grid. dots crowded near zero where the weights actually live. Sparse near the edges where weights rarely venture. NF4 has more grid points where the action is. Per group scales contain the outlier. The smart grid spends every bit of resolution where the bulk is. Together they make production 4bit possible. You may have heard of GPTQ or AWQ or LLM. int 8 or smooth quant or the kqus in gguf files five algorithms from the past four years they look completely different llm.int8 int 8 from 2022 splits the math. Outlier dimensions go through a separate 16- bit path. Everything else stays at 8 bits. Smooth quant uses an algebraic identity. The mat x * w= x / s * s * w for any factor s. So we can shrink the activations and grow the weights by the same factor without changing the result. This migrates the difficulty of outliers from activations to weights. GPTQ quantizes one column at a time and pushes the leftover rounding error into the columns it has not touched yet. So errors cancel instead of accumulating. AWQ identifies the 1% of weight channels that multiply against the largest activations and scales those channels up before rounding so they hit a more accurate grid point. storage stays uniform and the GGFK quants take per group scales and double quantize them. Five mechanisms, five looks. What problem are they all solving? Outliers, every single one. They are five different ways to handle the same villain. Some change the precision boundary. Some use the matal identity. Some redistribute rounding error. But they all exist because beyond a certain scale, a tiny handful of weights and activations dominate the dynamic range. Once you see this, the field of LLM quantization stops looking like a zoo of disconnected tricks. It becomes one technique with several flavors. What does this look like in practice? When you download a quantized model today, the file name has a quantization suffix like Q4KM. Every character means something. The Q means quantized. The four means four bits per weight. The K means kant per group scales in a two-level block structure. Exactly the technique we just described. The M means medium variant where bulk weights stay at four bits, but a few sensitive tensors get bumped to five or six bits. Four characters, one precise recipe. Does it work on Llama 38B? At 16 bits, perplexity is 6.23 on 15 GB. At 8 bits, perplexity is still 6.23 on 8 GB. At Q5KM, 6.29 for 5.3 GB. At Q4KM, perplexity is 6.38 on 4.58 GB. 2.4% quality cost, 3.3 times size savings, and the 70 billion parameter version at Q4KM about 42 GB. It runs on a single 48 GB consumer GPU. The full 16-bit version needs two A100 GPUs, each costing more than $10,000. Same model, same architecture, same weights. The only thing that changed is how they are stored. Two persistent myths still circulate. Let's look at each one. Myth one, quantization halves model quality. The data says otherwise. On llama 38B, Q4KM loses about 2.4% 4% perplexity and 1.5 to 3 MMLU points for 3.3 times less memory. The accuracy versus bits curve is shaped like a hockey stick. Flat from 16 down to five, a small dip at four, a cliff below three. Stay above the cliff and the cost is small. Myth two, AWQ keeps salient weights at 16 bits. It doesn't, but the misconception is understandable. Every weight in an AWQ model is stored at uniform four bits. Salient channels are protected by a numerical trick. Multiply them by a per channel factor before rounding so they hit a more accurate grid point. Divide the activations by the same factor after the maple. The output is unchanged. The storage stays uniform. Both myths come from old intuitions extended to a regime where they no longer hold. We started with a number 280 GB bigger than any laptop bigger than four GPUs combined. Now we know how it shrinks. LLM.int8 and Smooth Quant cut the storage in half twice. GPTQ, AWQ, and the GGUFK quants take it down to four bits with NF4 giving them a smarter grid to round to. Five algorithms, one root cause. The same outlier weights that broke naive 8bit handled five different ways. The result is a door between the data center where models are trained and the laptop where they're used. And there is something genuinely beautiful in that. The frontier keeps opening it. Bitnet uses turnary weights. FP4 microscaling pushes 4bit floats into hardware. The numbers will keep getting smaller. The bell curve of weights stays the same.