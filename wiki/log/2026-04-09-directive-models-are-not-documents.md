---
title: "Models Are Not Documents — They Must Be Usable Systems"
type: note
domain: log
note_type: directive
status: active
confidence: high
created: 2026-04-09
updated: 2026-04-09
sources: []
tags: [log, directive, models, quality, depth, wikilinks]
---

# Models Are Not Documents — They Must Be Usable Systems

## Summary

The 14 "model entry points" are mindless documents — lists of page paths that don't use wikilinks and don't actually articulate what a model IS. A model is not a bibliography of related pages. A model is a usable system with clear structure, standards, schemas, and actionable guidance.

## Operator Directive (verbatim)

> I see a lot of nonlink for things that should be link in obsidian.
> e.g. "notebooklm-py CLI ([[notebooklm-py CLI]])"
> 
> and the:
> Relationships
> BUILDS ON: NotebookLM
> BUILDS ON: notebooklm-py CLI
> RELATES TO: Model: Automation + Pipelines
> 
> There is also no trace of what I asked in the model.. it look like mindless document.... do you not know what a model is ?

## What Failed

1. **No wikilinks** — pages reference other pages by file path (`wiki/domains/...`) instead of [[wikilinks]]. In Obsidian this means zero graph connectivity from the model entry points.
2. **Models are reading lists, not systems** — they list "read this page, then this page" but don't articulate the model itself. What IS the model? What are its principles? What are its standards? What schemas does it define? How do you APPLY it?
3. **No trace of the user's directives** — the user asked for specific things in each model (e.g., methodology flexibility, composable sub-models, per-case artifact chains) and none of that appears in the model pages.
4. **A model must be a usable system** — with its own standards, norms, schemas, entry points, and actionable guidance. Not a list of wiki pages.

## What a Model Actually Is

A model is NOT a reading list. A model is:
- A named, coherent SYSTEM with clear boundaries
- Standards and norms that define how it works
- Schemas (if applicable) that enforce structure
- Actionable guidance: "when you encounter X, do Y"
- Examples from practice (instances, case studies)
- Clear connection to the super-model (methodology framework)
- Wikilinks throughout for Obsidian graph navigation

## Relationships

- FEEDS INTO: [[Models Are Built in Layers, Not All at Once]]

## Backlinks

[[Models Are Built in Layers, Not All at Once]]
