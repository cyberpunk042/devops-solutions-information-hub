# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Project
*.pyc

# Obsidian workspace (user-specific, not portable)
wiki/.obsidian/workspace.json
wiki/.obsidian/workspace-mobile.json

# Evolution queue prompt files (generated, not committed)
wiki/.evolve-queue/*.prompt.md

# Auto-scaffolded pattern stubs from scorer (low-value, need manual curation)
wiki/patterns/pattern-*-+-*.md

.sync-state.json

# Machine-specific MCP config (regenerated per-machine via `python3 -m tools.setup --init`)
# The template .mcp.json.template is tracked as reference.
.mcp.json

# Per-user Claude Code settings (allowlists, etc.)
.claude/settings.local.json

scheduled_tasks.lock

# =============================================================================
# Per-system runtime state surfaces (operator 2026-05-09 turn 8)
# =============================================================================
# .cursor/ — common cross-project state surface for /view + /questions + vision
#            + status + tracking + trace + progress + focus + mission. Auto-
#            regenerated by `tools/state.py` (or assistant CRON job). Per-system
#            temp; not cross-machine. Aligns with root-modules cursor-files
#            convention. See wiki/spine/standards/cursor-state-folder-standards.md
.cursor/

# .assistant/_state/ — Per-Project Assistant runtime state (not the Profile YAMLs
#                     which ARE tracked; just the runtime state subdir).
.assistant/_state/

.openclaw/